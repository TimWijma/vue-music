export class Song {
    title: string;
    artist: string;
    cover: string;

    constructor(title: string, artist: string, cover: string) {
        this.title = title;
        this.artist = artist;
        this.cover = cover;
    }
}
